{
  "name": "seller-re-nestjs",
  "version": "0.0.1",
  "description": "Seller-Re",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start:backend": "cd backend && npm run start",
    "start:backend:dev": "cd backend && npm run start:dev",
    "build:backend": "cd backend && npm run build"
  }
}
